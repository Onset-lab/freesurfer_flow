params {
    run_yale_atlas = true
}

process {
    withName: "YALE_ATLAS" {
        cpus = 1
        memory = { task.attempt * 10.GB }
        time = { task.attempt * 2.h }
        ext.when = params.run_yale_atlas
        ext.atlas_utils_folder="/Yale_atlas_utils/"
    }
}
