params {
    run_lausanne_atlas = true
}

process {
    withName: "LAUSANNE_ATLAS" {
        cpus = 1
        memory = { task.attempt * 20.GB }
        time = { task.attempt * 3.h }
        ext.when = params.run_lausanne_atlas
        ext.atlas_utils_folder="/FS_BN_GL_SF_utils/"
    }
}