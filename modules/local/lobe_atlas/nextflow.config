params {
    run_lobe_atlas = true
}

process {
    withName: "LOBE_ATLAS" {
        cpus = 4
        memory = { task.attempt * 20.GB }
        time = { task.attempt * 3.h }
        ext.when = params.run_lobe_atlas
        ext.atlas_utils_folder="/FS_BN_GL_SF_utils/"
    }
}