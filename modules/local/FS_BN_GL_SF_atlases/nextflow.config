params {
    run_atlases = true
}

process {
    withName: "FS_BN_GL_SF_ATLASES"{
        cpus = 4
        memory = { task.attempt * 20.GB }
        time = { task.attempt * 3.h }
        ext.when = params.run_atlases
        ext.atlas_utils_folder="/FS_BN_GL_SF_utils/"
    }
}