nextflow.enable.moduleBinaries = true

params {
    help=false

    fs_input=false
    fs_output=false
    fs_license=false

    output_dir="results_fs"
}

process {
    tag = {"$meta.id"}
    publishDir = [path: {"${params.output_dir}/$meta.id/${task.process.split(':')[-1]}"}, mode: 'symlink']
    scratch = true
    stageInMode = 'copy'
    stageOutMode = 'rsync'
    cache = 'lenient'
    maxRetries = 3
    maxErrors = -1
    errorStrategy = { task.attempt <= 3 ? 'retry' : 'ignore' }
}

includeConfig "modules/local/FS_BN_GL_SF_atlases/nextflow.config"
includeConfig "modules/local/lobe_atlas/nextflow.config"
includeConfig "modules/local/lausanne_atlas/nextflow.config"
includeConfig "modules/local/yale_atlas/nextflow.config"
includeConfig "modules/local/fastsurfer/nextflow.config"

profiles {
    docker_use_gpu {
        docker.enabled          = true
        conda.enabled           = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
        docker.runOptions = '-v /var/run/docker.sock:/var/run/docker.sock --entrypoint "" --gpus all --rm --user $(id -u):$(id -g) --network host'
        process {
            withName: "FASTSURFER" {
                maxForks = 1
            }
        }
    }
    slurm {
        process {
            executor = 'slurm'
            errorStrategy = 'retry'
            maxRetries = 1
            maxForks = 500
        }
        executor {
            pollInterval = '180 sec'
            queueGlobalStatus = true
            queueStatInterval = '3 min'
            submitRateLimit = '100/1min'
            queueSize = 500
        }
    }
    docker {
        docker.runOptions = '-v /var/run/docker.sock:/var/run/docker.sock --entrypoint "" --rm --user $(id -u):$(id -g) --network host'
        docker.enabled          = true
        conda.enabled           = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
    }
    apptainer {
        apptainer.enabled       = true
        apptainer.autoMounts    = true
        conda.enabled           = false
        docker.enabled          = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
    }
    apptainer_use_gpu {
        apptainer.enabled       = true
        apptainer.autoMounts    = true
        conda.enabled           = false
        docker.enabled          = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        apptainer.runOptions    = '--nv'
        process {
            withName: "FASTSURFER" {
                maxForks = 1
            }
        }
    }
    singularity {
        singularity.enabled       = true
        singularity.autoMounts    = true
        conda.enabled           = false
        docker.enabled          = false
        apptainer.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
    }
    singularity_use_gpu {
        singularity.enabled       = true
        singularity.autoMounts    = true
        conda.enabled           = false
        docker.enabled          = false
        apptainer.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        singularity.runOptions    = '--nv'
        process {
            withName: "FASTSURFER" {
                maxForks = 1
            }
        }
    }
}

manifest {
    name = 'Freesurfer Flow'
    description = 'Compute Freesurfer and other atlases'
    version = '1.0.0'
    homePage = 'https://github.com/Onset-lab/freesurfer_flow'
    nextflowVersion = '26.04.6'
}